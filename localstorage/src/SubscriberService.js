export default class SubscriberService {
    constructor() {
        console.log(`👁️ [SubscriberService.js] initialized`);
    }

    async saveEmail(email) {
        console.log(`👁️ [SubscriberService.js] ${email}`);
    }
}